// este js carga las partes mas importantes de la aplicación como es el side bar, las partes de los creates, el wall y el chat

// load wall

//$(document).ready(function(){
	//alert('alert');
    // se manda a llamar el documento titulado load_wall para que cargue dinámicamente todos los archivos del wall
      // $.getScript( "/load_layout/load_wall" );
        // se manda a llamar al menu de post
        //$.getScript( "/load_layout/load_post_menu" );

        // se manda a llamar al sidebar
        //$.getScript( "/load_layout/load_sidebar" );

        // se manda a llamar el load chat
        //$.getScript( "/load_layout/load_chat" );
//})