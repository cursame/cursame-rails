<%= form_for(@modul) do |f| %>
  <% if @modul.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modul.errors.count, "error") %> prohibited this modul from being saved:</h2>

      <ul>
      <% @modul.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br />
    <%= f.number_field :role_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
