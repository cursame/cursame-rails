<%= form_for(@members_in_group) do |f| %>
  <% if @members_in_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@members_in_group.errors.count, "error") %> prohibited this members_in_group from being saved:</h2>

      <ul>
      <% @members_in_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :group_id %><br />
    <%= f.number_field :group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
