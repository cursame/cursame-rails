<% if !@error %>
  resetForm($('.delivery-form'));
  $('#nested_evaluation_form').empty();
  $('#total_nested_evaluation').remove();
  $('#new_delivery_form').find('.upload-form').remove();
  $('#timeline').prepend("<%= escape_javascript(render(:partial => '/shared/publications', :locals => {:wall => @publication,:visible_for => 'network'}))%>");
  $('#timeline').effect("highlight", {}, 1000);

  $(".delivery-form input[name='files[]']").remove();

  /* elimina los contenidos*/
  $(".content_free").html('');
  $(".content_wikipedia").html('');
  $(".content_killer").html('');
  $(".choser").html('');
  $('.youtube_content_delivery').hide();

  Notice('success', 'Tarea creada correctamente.');
  
  CleanerBlur();
  DisableBlured();

  $('#pk_menu').html('<%= escape_javascript(render(:partial => 'shared/post_menu')) %>');
  cancelButtonsPostMenu();  
  
<%else%>
  Notice('error', 'Algo ha pasado que no se pudo crear la tarea.');
<%end%>