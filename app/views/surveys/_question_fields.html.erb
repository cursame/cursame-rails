<div class="fields">
    <%= link_to_add_fields "Agregar respuesta", f, :answers, {:class=>'a-btn'} %> <br /><br />
    <%= f.text_area :content, :rows => 3 %><br />
    <%= f.label :value, "Valor" %>
    <%= f.text_field :value%>
    <b>archivo</b>

  <%= f.fields_for :answers do |builder| %>
    <%= render 'answer_fields', :f => builder %>
  <% end %>
  <%= link_to_remove_fields "remove", f, {:class=>'remove-icon', :style => 'both'}  %>
</div>