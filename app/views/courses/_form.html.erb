<%= form_for(@course, :html => { :class => "default-form"}, :remote => true) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  Avatar:<%= f.file_field :avatar%></br>
  Imagen Principal: <%= f.file_field :coverphoto%> 
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :silabus %><br />
    <%= f.text_area :silabus %>
  </div>
  <div class="field">
    <%= f.label :init_date %><br />
    <%= f.datetime_select :init_date %><br />
  </div>
  <div class="field">
    <%= f.label :finish_date %><br />
    <%= f.datetime_select :finish_date %><br />
  </div>
  <div class="field">
        Tipo de curso: <%= f.select :public_status, options_for_select([['Público', 'public'],['Privado', 'Private']])%><br />
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
