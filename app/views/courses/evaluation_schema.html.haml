- content_for :javascripts do
  = javascript_include_tag 'courses/course-evaluation-schema'

= render :partial => 'courses/show/course-header', locals: { header_style: :super_compact }
.wrapper
  .cols-12
    .col.col-12
      .col-inner
        .section-help
          = icon 'pie-chart'
          %p Especifica como quieres evaluar este curso, puedes agregar nuevos criterios de evaluación o solo dejar Cúrsame.
        .course-evaluation-schema
          = form_for @course, method: :put, html: { class: 'evaluation-form-js' } do |f|
            - course_current_evaluation = 0
            - @course.evaluation_criteria.each do |criteria|
              - course_current_evaluation += criteria.evaluation_percentage
            .course-evaluation-schema-list
              %table.table-schema
                %thead
                  %td Criterio
                  %td Porcetaje
                %tr
                  %td
                    %h5 Cursame
                  %td.eva-percenage
                    %h5.cursame-evaluation-percentage
                      %span= 100 - course_current_evaluation
                      = "%"
                = f.fields_for :evaluation_criteria do |builder|
                  = render 'courses/shared/course_evaluation_schema_fields', f: builder

              .add-new-criteria
                = link_to_add_course_evaluation_fields 'Agregar criterio de evaluación', f, :evaluation_criteria
            .course-evaluation-schema-footer
              %ul.list-buttons.right.fr
                %li= link_to 'Cancelar', course_path(@course), class: 'btn btn-medium btn-secondary'
                %li
                  %span.btn.btn-medium.btn-primary.input-btn= submit_tag "Guardar"


