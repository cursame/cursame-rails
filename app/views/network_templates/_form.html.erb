<%= form_for(@network_template) do |f| %>
  <% if @network_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@network_template.errors.count, "error") %> prohibited this network_template from being saved:</h2>

      <ul>
      <% @network_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :network_id %><br />
    <%= f.number_field :network_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
