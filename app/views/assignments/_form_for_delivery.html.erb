<%= form_for(@assignment = Assignment.new, :html => { :class => "default-form assignment-delevery-form"}) do |f| %>
   <%= f.hidden_field :course_id, :value => @course_id %>
   <%= f.hidden_field :delivery_id, :value => @delivery_id %>
   
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :brief_description %><br />
    <%= f.text_area :brief_description %>
  </div>
    <%= f.hidden_field :user_id, :value => current_user.id%>
  <div class="field">
  </div>
  <div class="field">
    <%#= f.label :accomplishment %><br />
    <%#= f.number_field :accomplishment %>
  </div>
     <%# @killer = @assignment.assets %>
    <%# super_asset = @killer.build %>

    <%#= f.fields_for (:assets),:html => { :id => "assignments" } do |asset|  %>
          <%#= render :partial => "shared/assets_form", :locals => { :f => asset }%>
    <%#end%>

  <div class="actions">
    <%= f.submit %>
  </div>

   <%#= f.text_field :title, :placeholder=>'Título' %>
   <%#= f.text_area :brief_description, :rows => 6, :cols => 78, :placeholder=>'Entrega' %>
   <%# @killer = @assignment.assets %>
   <%# super_asset = @killer.build %>
   <%#= f.submit "Entregar" %>
    <%#= button_tag "Cancelar", :type => 'button', :class=>'input-button', :id => 'cancel-post-form' %>
   <%#= f.fields_for (:assets),:html => { :id => "assignments" } do |asset|  %>
    <%#= render :partial => "shared/assets_form", :locals => { :f => asset }%>
   <%#end%>

<% end %>
