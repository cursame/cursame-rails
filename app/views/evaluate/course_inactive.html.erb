<%= render partial: 'evaluate/shared/header_evaluate_course', locals: { course: @course } %>
<div class="activities-section">
	<div class="wrapper">
		<div class="cols-12">
			<div class="col col-12">
				<div class="col-inner">
					<div class="activities-list-section">
						<% unless @activities.empty? %>
							<%= render partial: 'evaluate/shared/activities_list', locals: { activities: @activities } %>
						<% else %>
							<div class="no-content-section">
				        <h4><%=t('.no_score')%></h4>
				        <p><%=t('.enrollment')%></p>
				      </div>
						<% end %>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
