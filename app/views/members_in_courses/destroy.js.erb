<% if !@channel.nil? %>
  var channel = "<%= @channel.channel_name %><%= @channel.channel_type == 'Course' ? '?course=true' : '' %>";
  console.log(channel);
  Chat.removeActiveChannel( channel )
<% end %>
window.location = "<%= courses_path %>"
