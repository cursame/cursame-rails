<% unless @discussion.nil? %>
  createModal('<%= escape_javascript(render(partial: "discussions/shared/discussion_modal", locals: { discussion: @discussion }))%>');
<% else %>
  Notice('error', I18n.t("modal.discussion_modal.discussion_not_found",{locale: "<%= I18n.locale %>"}));
<% end %>
