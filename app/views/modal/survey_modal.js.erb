<% unless @survey.nil? %>
	createModal('<%= escape_javascript(render(partial: "surveys/shared/survey_modal", locals: { survey: @survey }))%>');
<% else %>
	Notice('error', 'El cuestionario que tratas de ver ya no existe, ponte en contacto con el profesor del curso.')
<% end %>
