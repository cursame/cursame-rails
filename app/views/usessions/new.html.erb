
<% if current_network.nil? %>
  <div class="content login confirmationIndexHeader" id="titlheader">
    <p>Lo sentimos, la red a la que estas intentado accesar no existe
    pregunta a un encargado de tu escuela él sabrá ayudarte.</p>
    <%= link_to "Ir al inicio", "http://www.cursa.me/browser/" %>
  </div>
<% else %>

  <% if !@user_inactive.nil? %>
    <p>Se ha enviado un correo a <%= @user_inactive.email %>. Esperando confirmación</p>
  <% end %>

  <div class="user-sessions-wrap">
    <div class="user-sessions-network-brand">
      <% if current_network.free || current_network.logo.file == nil %>
        <%= link_to icon('logo-cursame'), root_path, title: current_network.name, class: "free_network" %>
      <% else %>
        <%= link_to image_tag("#{current_network.logo.square}"), "http://#{current_network.subdomain}.#{links}/"%>
      <% end %>
    </div>
    <div class="user-sessions-main">
      <div class="user-session-content">
        <h4>Iniciar sesión</h4><br>
        <%= form_for(resource, :as => resource_name, :url => session_path(resource_name),:class => "form-sessions form-signin" ) do |f| %>
          <%= devise_error_messages! %>
          
          <div class="form-group no-label">
            <div class="form-group-main">
              <span class="input-icon session-icon-input">
                <%= icon('email') %>
                <%= f.email_field :email,:placeholder =>"Correo electrónico" %>
              </span>
            </div>
          </div>

          <div class="form-group no-label">
            <div class="form-group-main">
              <span class="input-icon session-icon-input">
                <%= icon('password') %>
                <%= f.password_field :password, :placeholder =>"Contraseña"%>
              </span>
            </div>
          </div>

          <div class="user-sessions-footer">
            <div class="fl">
              <% if devise_mapping.rememberable? -%>
                <label class="remember">
                  <%= f.check_box :remember_me %> &nbsp;Recordarme
                </label>
              <% end %>
            </div>
            <div class="fr">
              <span class="btn btn-primary btn-medium input-btn">
                <%= f.submit "Iniciar Sesión" %>
              </span>
            </div>
          </div>
          
          <% end %>
      </div>

      <div class="user-session-links">
        <div class="links">
          <%= render "devise/shared/links" %>
          <a href="#" id="linked" >Soy un usuario antiguo y tengo dudas</a>
        </div>

        <div id="sub_linked" style="display: none;">
          <h5>¿Soy usuario?</h5>
          <p>Si eras usuario en la plataforma anterior aún sigues siendo usuario en Cúrsame.</p>
          <br>
          <h5>¿Mi clave sigue siendo la misma?</h5>
          <p>Se han generado nuevas contraseñas cuando confirmes tu correo electrónico con el mail que enviamos te pedirá que cambies tu contraseña.</p>
          <br>
          <h5>No me ha llegado el mail de confirmación.</h5>
          <p>Da click en el link <%= link_to "Confirma tu cuenta", new_unlock_path(resource_name) %> te enviará a una pantalla que te pedirá tu email para que te volvamos a enviar los datos de confirmación.</p>
          <br>
          <h5>Confirmé mi email pero olvide cambiar mi contraseña</h5>
          <p>Cualquier problema con la contraseña puedes resolverlo dando click en <%=link_to "Olvidé mi contraseña", new_user_password_path %> donde se te enviará un mail para que puedas cambiar tu contraseña.</p>
          <br>
          <a href="#" id="Closelinked">Cerrar</a>
        </div>
      </div>

    </div>
  </div>
  
<% end %>

<script>
  $('#linked').click(function(event) {
    event.preventDefault();
    $('#sub_linked').slideToggle();
  });

  $('#Closelinked').click(function(event) {
    event.preventDefault();
    $('#sub_linked').slideUp();
  });
</script>
