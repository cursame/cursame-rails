.post-meta-info
  .post-meta-item{ data: { wall_like_id: wall.id } }
    - unless current_user.voted_up_on? post
      = link_to(t('.like'), upvote_path(wall.id), remote: true)
    - else
      = link_to(t('.not_like'), downvote_path(wall.id), remote: true)