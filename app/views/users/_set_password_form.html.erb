<div class="overlay-header">
  <div class="overlay-title">
    <h4>Establece tu contraseña</h4>
  </div>
  <div class="close-overlay"><i class="i i-x-close"></i></div>
</div>
<div class="overlay-content">

	<div id="set-password-form">
		<p>Bienvenido a Cúrsame, es necesario que establezcas una contraseña para terminar con el proceso de registro.</p><br>
		<%= form_tag "/users/set_password", remote: true do%>
			<%= hidden_field_tag 'user_id', user.id %>
			<div class="form-group-main">
        <span class="input-icon session-icon-input">
					<%= icon('password') %>
          <%= password_field_tag :password, "", placeholder: "Ingresa una contraseña." %>
        </span>
      </div>
		<% end %>
	</div>
</div>
<div class="overlay-footer">
	<span class="btn btn-medium btn-primary input-btn">
		<%= submit_tag "Guardar contraseña" %>
	</span>
</div>