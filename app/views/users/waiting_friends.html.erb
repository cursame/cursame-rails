
<h1><%=t('.friends')%></h1>
<% if @user_two != nil %>
<p><%=t('.waiting_friends')%>:<%= image_tag(@user_two.avatar.mini)%> <b><%= link_to "#{@user_two.name} #{@user_two.email}", show_user_path(@user_two.personal_url) %></b></p>

<%end%>
<% if @user_one_two != nil%>
<p><%=t('.request')%>:<%= image_tag(@user_one_two.avatar.mini)%> <b><%= link_to "#{@user_one_two.name} #{@user_one_two.email}", "/users/#{current_user.personal_url}/waiting_friends/#{@relation_two.id}" %></b></p>
   
<%end%>
