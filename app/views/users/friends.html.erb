
<%= render partial: 'user-header', locals: { compact: false } %>

<div class="wrapper spaced">
  <div class="sub-profile-title">
    <h3>Amigos</h3>
  </div>
</div>
<% if @user_l == current_user %>
<%= link_to "Todos mis amigos", user_friends_path%> |
<%= link_to "Solicitudes pendientes", pendding_friends_path(@user_l.personal_url), :remote => true %>
<%else%>
<%#= link_to "Amigos en Común", user_friends_path%>
<%end%>
<div id="accesed">
<%= render :partial => 'users/friends_tpl', :locals => {:friends => @friends} %>
</div>
