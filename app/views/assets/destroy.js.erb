Notice('<%= @flash[:type] %>', '<%= t @flash[:message] %>');
<% if @flash[:type] == 'success' %>
  var fileBox = $('div.support-material-item#<%= @asset.id %>');
  fileBox.slideUp(200, function () {
    $(this).remove();
  });
<% end %>