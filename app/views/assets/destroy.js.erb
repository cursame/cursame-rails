<% if @error %>
  Notice('error', 'Estas intentando borrar un archivo que no es tuyo');
<% else %>
  var fileBox = $('div.support-material-item#<%= @asset_id %>');
  fileBox.slideUp(200, function() {
    $(this).remove();
  });
<% end %>