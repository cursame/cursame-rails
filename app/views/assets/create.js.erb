var supportMaterialBox = $('div.support-material#<%= params[:from] %>-assets'),
    content = '<%= escape_javascript(render( partial: "assets/asset_file", locals: { asset: @asset } ))%>';

supportMaterialBox.append(content);

console.log("<%= @asset.id%>", "<%= params[:from] %>");