.section-header
  .section-header-main
    .section-title
      = link_to icon('section-library'), library_path(current_network.library)
      = link_to library_path(current_network.library) do
        %h1="Biblioteca"
    .section-header-actions
      %ul.list-buttons.right
        %li= link_to "#{icon('plus-white')} Crear Carpeta".html_safe, new_library_directory_library_directory_path(@directory), title: "Crear Carpeta", class: "btn btn-primary"
        %li= link_to "#{icon('plus-white')} Subir Archivo".html_safe, new_library_directory_library_file_path(@directory), title: "Subir Archivo", class: "btn btn-primary"