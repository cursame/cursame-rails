$('#contact-form')[0].reset();
$('.other-lms').hide();

<% if @mkt.present? %>
  window.location = "<%= mkt_thanks_url %>?success=1";
<% else %>
  alert(I18n.t("home.send", {locale: "<%= I18n.locale %>"});
<% end %>
