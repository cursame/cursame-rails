$('#contact-form')[0].reset();
$('.other-lms').hide();

if ( $('.mkt-langing-pages').length ) {
  $('.mkt-page-main').hide();
  $('.thanks-message').show();
} else {
  alert('Tu mensaje se ha enviado!');
};