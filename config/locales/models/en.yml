en:
  activerecord:
    errors:
      messages:
        taken: "It's already taken"
        subdomain:
          invalid_network: "The network doesn't exists"
        csv_file:
          malformed: "The file is an invalid CSV"
          unknown_encoding: "The file doesn't have a valid encode"
          empty: "The file must contain at least one user"
  delivery:
    nil_end_date: No end date
    deadline: Deadline
  survey:
    nil_end_date: No end date
    deadline: Answer before
